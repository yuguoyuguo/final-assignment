须知：
    最左边数字是app.py对应第几行内容
    前面有空格是上面的额外解释
    task：任务
    count：计数 1,2,3
    tid:任务的ID
    t/uid_count：对应的计数
    register：注册
    loginout：登出
    delecte：删除
    某.append():将括号内容添加到 某  里面
    单词后面加s的表示所有的某某某

import xx 旁边的#会解释该import后面函数的作用
例如：
from cryptography.fernet import Fernet #加密工具
                                  ↑
加密工具函数是import后面的 Fernet

68：发现有代理IP = (真实IP,代理IP).split(",")[0]   
    .split(",")[0]：以 , 为分割点  将真实IP和代理IP 转为 真实IP=[0] 代理IP=[1]
    最后选择真实IP

106：text为明文的变量名  text=123   字节= b'内容'
    括号里的先执行然后执行括号外面的函数 encrypyt(text.encode())
    decode() 将字节转字符串   字符串= '内容'

160： global：让def外面的变量名的内容可以在def里面被直接修改
    def里面的变量名原本跟外面的变量名是独立的


214：def need_login()     字面意思 需要登录 
    因为它有句 @wraps(func) 在下一行
    它现在是一种可以直接把所在函数的元数据直接复制过来
    元数据：

225：@need_login  有个@ 它现在把def need_login功能应用在 def need_admin 
                在执行 def need_admin之前先执行 def need_login

        
244： @app.route("/login")    登录功能页面    methods=访问方法   
            下面一行就开始登录功能的实现
            其它@app.route("...")格式就是某某某的功能
     

        
具体功能大部分上面都有提过或字面意思
=============
总共分以下部分
Flask配置
日志配置写入
全局异常处理
安全工具
数据存储和计数器
装饰器
前端接口
启动