须知： 
    带#的那一行是上面一行的解释
    代码质量测试：bandit -r app.py -f json -o bandit_report.json  
    #-o：输出的文件名 -f 输出的文件类型 -r 读取app.py
    
运行app.py
测试访问网站http://127.0.0.1:6961/test.html

先往下滑是否只显示登录和注册
任意登录一个账号才会显示任务面板 创建和查看任务 只能看到自己的
#防止未知用户扰乱管理员面板显示

登录面板测试
    
    账号和密码只要错一个字符就会统一报错：用户名或密码错误
    #逻辑漏洞：防止黑客因确认有该用户名或密码爆破出完整账号

    管理员面板只有管理员登录状态下出现
    可以管理查看 所有用户&创建的所有任务

管理员账号
admin
Admin@123

注册面板测试
    注册一个管理员同名账号
    #防止因密码修改 导致将同名账号的密码一起修改
    
    注册报错会统一显示：注册信息无效
    #防止黑客因确认有该账号爆破出密码 或 将数据库中所有账户名全部撞出来得知有谁

==========================================
在app.py同一目录下会有
文件夹
    data：数据库
    logs：日志
    sessions：会话